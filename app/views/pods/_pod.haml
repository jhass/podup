%tr
  %td= "##{pod_counter+1}"
  %td= link_to pod.name, pod
  -unless skip_owner
    %td= link_to pod.owner.name, public_user_path(pod.owner)
  %td
    = "#{pod.reliability}%"
    - if pod.maintenance?
      %span#maintenance_notice_list
        Maintenance since
        = time_ago_in_words(pod.maintenance)
  %td= location(pod)
  %td= pod.score
  %td= stars(pod.stars)
  - if display_acceptation_state
    %td= acceptance_state(pod.accepted?)
